type List = {
  id: string;
  name: string;
  description: string;
  code: string;
};

type Subscription = {
  id: string;
  listId: string;
  number: string;
  status: string;
  createdAt: string;
};
